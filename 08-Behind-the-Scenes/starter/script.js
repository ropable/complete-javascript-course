'use strict';

function calcAge(birthYear) {
  const age = new Date().getFullYear() - birthYear;
  console.log(firstName);
  return age;
}

const firstName = 'Ashley';
let age = calcAge(1978);
console.log(age);
